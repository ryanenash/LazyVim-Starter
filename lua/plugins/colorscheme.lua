return {
  {
    "LazyVim/LazyVim",
    opts = {
      colorscheme = "tokyonight-night",
    },
  },
}
-- return {
--   "folke/tokyonight.nvim",
--   lazy = false, -- make sure we load this during startup if it is your main colorscheme
--   priority = 1000, -- make sure to load this before all the other start plugins
--   opts = {
--     -- The default style. You can change it to "storm", "night", "moon", or "day".
--     style = "storm",
--
--     -- Enable this to disable setting the background color
--     transparent = true,
--
--     -- You can configure other settings here, but the above are the most important
--     -- for your request. For example, to customize highlights:
--     styles = {
--       comments = { italic = true },
--       keywords = { italic = true },
--       -- Background styles. By default, these are unused when `transparent` is true.
--       sidebars = "transparent",
--       floats = "transparent",
--     },
--   },
-- }
